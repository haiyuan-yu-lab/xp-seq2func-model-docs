# RegressPredictor

Scalar regression prediction head for **v0.1.0a8**.

Nestable only under `EncoderPredictor.predictor`. Emits scalar predictions
written as `.pred.npy` by `pred_model`.

Coming in a later documentation slice: configuration, label geometry, and loss
pairing. See [Model composition](composition.md) and [Labels](../data/labels.md).
