# ConvEncoder

Convolutional encoder component for **v0.1.0a8**.

Nestable only under `EncoderPredictor`. Cannot be used as top-level
`model_type`.

Coming in a later documentation slice: configuration fields, hyperparameters,
inputs, and outputs. See [Model composition](composition.md) and
[Config](../config.md).
