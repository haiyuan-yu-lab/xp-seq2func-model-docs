# ClassPredictor

Classification prediction head for **v0.1.0a8**.

Nestable only under `EncoderPredictor.predictor`. Emits class predictions
consumed by training losses and `pred_model` `.pred_class.npy` artifacts.

Coming in a later documentation slice: configuration, label geometry, and loss
pairing. See [Model composition](composition.md) and [Labels](../data/labels.md).
