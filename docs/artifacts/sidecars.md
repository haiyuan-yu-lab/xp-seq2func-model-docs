# Hyperparameter sidecars

Hyperparameter sidecar artifacts for **v0.1.0a8**.

Training and tuning write JSON sidecars alongside checkpoints so prediction can
reload the same hyperparameters. See [Formats](../formats.md).

Coming in a later documentation slice: exact filenames and field contracts.
