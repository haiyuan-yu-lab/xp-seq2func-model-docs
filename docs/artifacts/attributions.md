# Attributions

Attribution array artifacts from `pred_model` in **v0.1.0a8**.

- Legacy mode writes per-head `attr_*.npy` files
- Explicit `--attribution-target` writes one target-qualified file with a
  shared **attribution target** and **output domain**

See [pred_model](../cli/pred_model.md) and
[Attribution workflow](../workflows/attribution.md).

Coming in a later documentation slice: filename grammar and shape tables.
