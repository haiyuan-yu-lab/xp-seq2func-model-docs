# Tuning spaces

Tune-space JSON for `tune_model` in **v0.1.0a8**.

Defines searchable leaves over hyperparameter paths. See
[tune_model](../cli/tune_model.md) and [Config](../config.md).

Coming in a later documentation slice: leaf types, nesting rules, and invalid
examples.
