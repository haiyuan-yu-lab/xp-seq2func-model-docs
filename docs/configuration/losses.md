# Losses

Loss objects paired with prediction heads in **v0.1.0a8**.

Supported `type` values summarized on [Config](../config.md) include
`categorical_cross_entropy`, `mse`, `profile_cross_entropy`, and `log1p_mse`.

Coming in a later documentation slice: per-loss params and head pairing rules.
