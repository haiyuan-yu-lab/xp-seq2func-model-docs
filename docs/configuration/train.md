# Train configuration

Train-config JSON for `train_model` in **v0.1.0a8**.

Current field summary lives on the preserved [Config](../config.md) page under
**Train config**.

Coming in a later documentation slice: dedicated required/optional tables,
defaults, and invalid examples for train-only keys.
